<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.upc.eetac.dsa.eetakemongoandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ultraball"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ultraball"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.SingIn"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.LogIn"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".Activities.Principal"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.EetakemonsCatched"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.ViewEetakemon"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.SelectEetackemon" />
        <activity
            android:name=".Activities.CatchEetakemon"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.Eetakedex"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GameClient.ClientRequest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.Fight"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.SelecUser"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.GameClientRequest" />
        <activity android:name=".View.ViewProfile" />
        <activity android:name=".View.ViewAtack"></activity>
        <activity android:name=".Activities.GameClientRequest" />
        <activity
            android:name=".Activities.TestClient"
            android:label="@string/title_activity_test_client"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>